<h1>Burgers!</h1>
<h2>Burgers that are not Devoured!</h2>
<ul>
  {{#each burgers}}
    {{#unless devour}}
      {{> burgers/burgers-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>
<h2>Burgers that are devoured!</h2>
<ul>
  {{#each burgers}}
    {{#if devour}}
      {{> burgers/burgers-block devour=false}}
    {{/if}}
  {{/each}}
</ul>
<h2>Add a Burger</h2>
<form class="create-form">
  <div class="form-group">
    <label for="ca">burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>
  <div class="form-group">
    <label for="Devoured">Devour?</label><br>
    <input type="radio" name="devour" value="1" checked> Devoured!<br>
    <input type="radio" name="devour" value="0"> Not Eaten
  </div>
  <button type="submit">Add Burger</button>
</form>